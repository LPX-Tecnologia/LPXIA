# Frontend

This directory contains the Next.js 15 frontend for LPX-NEXUS.

## Run locally

```bash
npm install
npm run dev
```

The app uses the App Router, TypeScript, Tailwind CSS, and simple API integration with the backend.
